---
import Layout from "../../layout/Layout.astro";

import GradingForm from "../../components/GradingForm.svelte";

// needed to be hardcoded for this project, because fetching from the api does not work in
// prod build, as the api is not running at astro build time
export function getStaticPaths() {
    return [
        { params: { id: 1 } },
        { params: { id: 2 } },
        { params: { id: 3 } },
    ];
}

const id = Astro.params.id;
---

<Layout>
    <!-- <AssignmentInfo {id} client:load />
    <GradingForm {id} client:load /> -->
</Layout>
